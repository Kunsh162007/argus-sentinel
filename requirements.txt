# ARGUS Sentinel — Dependencies
# Tested: Python 3.11 / 3.12 / 3.13
# Python 3.14: install with --pre flag if needed (see SETUP.md)

# ── Core LLM ──────────────────────────────────────────────────────
# Groq — free globally, no credit card, 6000+ tokens/min free
# Get free key at: https://console.groq.com
groq>=0.9.0
# Google Gemini — fallback if no Groq key
google-generativeai>=0.8.0

# ── Web Infrastructure ────────────────────────────────────────────
aiohttp>=3.10.0
async-timeout>=4.0
playwright>=1.44.0

# ── HTML parsing ─────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.2.0
python-dateutil>=2.9.0

# ── Dashboard ────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# ── Config / CLI ─────────────────────────────────────────────────
python-dotenv>=1.0.0
rich>=13.7.0

# ── Testing ───────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# ── LangChain (optional — only needed for alternative agent mode) ─
# langchain>=0.3.0
# langchain-anthropic>=0.3.0
