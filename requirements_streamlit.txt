# ARGUS Sentinel — Streamlit Cloud requirements
# Playwright is excluded here because Streamlit Cloud doesn't support it.
# The SiteWatcher agent gracefully degrades when the browser isn't available.

google-generativeai>=0.8.0
aiohttp>=3.10.0
async-timeout>=4.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
python-dateutil>=2.9.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-dotenv>=1.0.0
streamlit>=1.35.0
pandas>=2.0.0
